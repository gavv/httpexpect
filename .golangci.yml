linters:
  enable:
    - govet
    - revive
    - staticcheck
    - exhaustive
    - lll
    - misspell
    - dupword
  disable:
    - gosimple
    - unused

linters-settings:
  lll:
    line-length: 90

issues:
  exclude-rules:
    - text: unused-parameter
      linters:
        - revive
    - text: error-strings
      linters:
        - revive
    - text: indent-error-flow
      linters:
        - revive
